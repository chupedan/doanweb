@model IEnumerable<QLGO.Models.DONVITINH>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Tạo mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Mã đơn vị tính
        </th>
        <th>
            Tên đơn vị tính
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IDDVT)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TenDVT)
            </td>
            <td>
                @Html.ActionLink("Sửa", "Edit", new { id = item.IDDVT }) |
                @Html.ActionLink("Xem chi tiết", "Details", new { id = item.IDDVT }) |
                @Html.ActionLink("Xóa", "Delete", new { id = item.IDDVT })
            </td>
        </tr>
    }

</table>
